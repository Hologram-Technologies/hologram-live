[project]
name = "python-component-hello"
version = "0.1.0"
description = "Dependency-free Python Component v1 .holo example"
requires-python = ">=3.14,<3.15"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
