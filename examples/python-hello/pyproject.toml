[project]
name = "python-hello-holo"
version = "0.1.0"
description = "Small Python .holo execution example"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
