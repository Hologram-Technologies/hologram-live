package hologram:host@1.0.0;

/// Remove the next message from an admitted channel, or return none.
interface channel-subscribe {
  try-receive: func(channel: string) -> result<option<list<u8>>, string>;
}
