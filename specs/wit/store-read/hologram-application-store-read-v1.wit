package hologram:application-store-read@1.0.0;

/// The Hologram Component Model v1 guest interface with mediated object reads.
interface guest {
  enum error-code {
    invalid-input,
    failed,
  }

  record guest-error {
    code: error-code,
    message: string,
  }

  run: func(input: list<u8>) -> result<list<u8>, guest-error>;
}

world application {
  import hologram:host/store@1.0.0;
  export guest;
}
