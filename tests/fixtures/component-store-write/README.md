# Component store-write fixture

This fixture imports only `hologram:host/store-write@1.0.0`. Its input is an
ASCII κ, one newline, and the exact object bytes. It returns the κ after the
host accepts the content-addressed write.
